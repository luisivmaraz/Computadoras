<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.css" />
    <script src="https://code.jquery.com/jquery-1.11.1.min.js"></script>
    <script src="https://code.jquery.com/mobile/1.4.5/jquery.mobile-1.4.5.min.js"></script>
    <title>Abarrotes TI</title>

    <style>
        /* Estilo personalizado para el encabezado */
        .custom-header {
            background-color: red;
        }

        /* Estilo personalizado para el contenido del formulario */
        .custom-form-label {
            color: rgb(0, 0, 0);
        }

        /* Otros estilos personalizados según sea necesario */
    </style>
</head>
<body>
    <div data-role="page">
        <div data-role="header" data-position="fixed" class="custom-header">
            <h1>Abarrotes TI</h1>
            <div data-role="navbar">
                <ul>
                    <li><a href="#" class="ui-btn-active">One</a></li>
                    <li><a href="#">Two</a></li>
                    <li><a href="#">Three</a></li>
                </ul>
            </div>
        </div>

        <div data-role="content">
            <form action="/<%= product.barcode %>" method="POST">
                
                <label for="txt_codigo_barras" class="custom-form-label">Codigo de barras:</label>
                <input type="text" name="barcode" id="txt_codigo_barras" placeholder="Código de barras" 
                value="<%= product.barcode %>">
                
                <label for="txt_description" class="custom-form-label">Descripción:</label>
                <input type="text" name="description" id="txt_description" placeholder="Descripción" 
                value="<%= product.description %>">

                <!-- Otros campos del formulario con estilos personalizados -->

                <label for="txt_price" class="custom-form-label">Precio:</label>
                <input type="text" name="price" id="txt_price" placeholder="Precio" value="<%= product.price %>">

                <label for="txt_cost" class="custom-form-label">Costo:</label>
                <input type="text" name="cost" id="txt_cost" placeholder="Costo" value="<%= product.cost %>">

                <label for="txt_stock" class="custom-form-label">Stock:</label>
                <input type="text" name="stock" id="txt_stock" placeholder="Stock" value="<%= product.stock %>">

                <label for="txt_expiredDate" class="custom-form-label">Caducidad:</label>
                <input type="text" name="expiredDate" id="txt_expiredDate" placeholder="Caducidad" value="<%= product.expiredDate %>">

                <!-- Fin de campos adicionales -->

                <label for="flip-2" class="custom-form-label">Activo</label>
                <select name="status" id="flip-2" data-role="slider">
                    <option value="0" <%= product.status=0 ? "selected" : "" %> >No</option>
                    <option value="1" <%= product.status=1 ? "selected" : "" %> >Si</option>
                </select>

                <label for="submit-6" class="custom-form-label">Enviar</label>
                <button type="submit" id="submit-6" class="ui-shadow ui-btn ui-corner-all 
                    ui-mini" style="color: rgb(0, 0, 0);">Guardar</button>
                <a href="delete/<%= product.barcode %>" class="ui-btn ui-btn-b ui-corner-all ui-mini">Eliminar</a>

                <!-- Botón de Cancelar -->
                <a href="#" class="ui-btn ui-btn-a ui-corner-all" data-rel="back">Cancelar</a>
            </form>
        </div>

        <div data-role="footer">
            <h5 style="color: rgb(0, 0, 0);"> Luisivmaraz 03 </h5>
        </div>
    </div>
</body>
</html>
